#include "main.h"

/**
 * mul - multiplies two integers
 * @num1: first (int) number to be multiplied
 * @num2: second (int) number to be multiplied
 * Return: the result of the multiplication
 */
int mul(int num1, int num2)
{
	int result;

	result = num1 * num2;
	return (result);
}
